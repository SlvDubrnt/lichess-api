apiVersion: v1
kind: Service
metadata:
  name: {{ include "lichess-api.fullname" . }}-mongodb
  labels:
    app.kubernetes.io/name: {{ include "lichess-api.name" . }}-mongodb
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    name: mongodb
  selector:
    app.kubernetes.io/name: {{ include "lichess-api.name" . }}-mongodb

