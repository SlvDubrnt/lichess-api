apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name:  {{ include "lichess-api.fullname" . }}-mongodb-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path
