apiVersion: v1
kind: Service
metadata:
  name: {{ include "lichess-api.fullname" . }}-caddy
  labels:
    app.kubernetes.io/name: {{ include "lichess-api.name" . }}-caddy
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app.kubernetes.io/name: {{ include "lichess-api.name" . }}-caddy

